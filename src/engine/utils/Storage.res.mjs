// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Float from "@rescript/core/src/Core__Float.res.mjs";
import * as Core__Option from "@rescript/core/src/Core__Option.res.mjs";

function getString(key) {
  return Caml_option.nullable_to_opt(localStorage.getItem(key));
}

function setString(key, value) {
  localStorage.setItem(key, value);
}

function getNumber(key) {
  var str = Caml_option.nullable_to_opt(localStorage.getItem(key));
  if (str === undefined) {
    return ;
  }
  var value = Core__Float.fromString(str);
  if (value !== undefined && !isNaN(value)) {
    return value;
  }
  
}

function setNumber(key, value) {
  var value$1 = value.toString();
  localStorage.setItem(key, value$1);
}

function getBool(key) {
  var str = Caml_option.nullable_to_opt(localStorage.getItem(key));
  if (str === undefined) {
    return ;
  }
  var lower = str.toLowerCase();
  if (lower === "true") {
    return true;
  } else if (lower === "false") {
    return false;
  } else {
    return ;
  }
}

function setBool(key, value) {
  localStorage.setItem(key, value ? "true" : "false");
}

function getObject(key) {
  var str = Caml_option.nullable_to_opt(localStorage.getItem(key));
  if (str === undefined) {
    return ;
  }
  try {
    return JSON.parse(str);
  }
  catch (exn){
    return ;
  }
}

function setObject(key, value) {
  var value$1 = Core__Option.getOr(JSON.stringify(value), "");
  localStorage.setItem(key, value$1);
}

export {
  getString ,
  setString ,
  getNumber ,
  setNumber ,
  getBool ,
  setBool ,
  getObject ,
  setObject ,
}
/* No side effect */
