import {
  fromInitializer
} from "./chunk-QQX3ENIJ.js";
import "./chunk-L2ZSLUY3.js";
import "./chunk-G3PMV62Z.js";

// node_modules/@rescript/core/src/Core__Int.res.mjs
function fromString(x, radix) {
  var maybeInt = radix !== void 0 ? parseInt(x, radix) : parseInt(x);
  if (isNaN(maybeInt) || maybeInt > 2147483647 || maybeInt < -2147483648) {
    return;
  } else {
    return maybeInt | 0;
  }
}
function abs(x) {
  if (x >= 0) {
    return x;
  } else {
    return -x | 0;
  }
}
function range(start, end, optionsOpt) {
  var options = optionsOpt !== void 0 ? optionsOpt : {};
  var isInverted = start > end;
  var n = options.step;
  var step;
  if (n !== void 0) {
    if (n !== 0) {
      step = n;
    } else {
      if (start !== end) {
        throw new RangeError("Incorrect range arguments");
      }
      step = n;
    }
  } else {
    step = isInverted ? -1 : 1;
  }
  var length;
  if (isInverted === step >= 0) {
    length = 0;
  } else if (step === 0) {
    length = options.inclusive === true ? 1 : 0;
  } else {
    var range$1 = isInverted ? start - end | 0 : end - start | 0;
    var range$2 = options.inclusive === true ? range$1 + 1 | 0 : range$1;
    length = Math.ceil(range$2 / abs(step)) | 0;
  }
  return fromInitializer(length, (function(i) {
    return start + Math.imul(i, step) | 0;
  }));
}
function rangeWithOptions(start, end, options) {
  return range(start, end, options);
}
function clamp(min, max, value) {
  var value$1 = max !== void 0 && max < value ? max : value;
  if (min !== void 0 && min > value$1) {
    return min;
  } else {
    return value$1;
  }
}
function lnot(x) {
  return x ^ -1;
}
var Bitwise = {
  lnot
};
var Constants = {
  minValue: -2147483648,
  maxValue: 2147483647
};
export {
  Bitwise,
  Constants,
  clamp,
  fromString,
  range,
  rangeWithOptions
};
//# sourceMappingURL=@rescript_core_src_Core__Int__res__mjs.js.map
